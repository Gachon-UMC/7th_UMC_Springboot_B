#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/umc7th
#    username: root
#    password:
#
##  jpa:
##    hibernate:
##      ddl-auto: create
##    properties:
##      hibernate:
##        # show_sql: true
##        format_sql: true
##
##
##  logging.level:
##    org.hibernate.SQL: debug
##  # org.hibernate.type: trace #스프링 부트 2.x, hibernate5
##  # org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6


# local DB 사용
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study
    username: root
    password: ssh159357!
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: update # 처음에만 create, 나중에는 update. (create면 기존 테이블 모두 없애버리고 새로 만들게 됨)
        default_batch_fetch_size: 1000

## RDS 사용
#spring:
#  datasource:
#    url: jdbc:mysql://zipdabang-db.claedpluhwyw.ap-northeast-2.rds.amazonaws.com:3306/study
#    username: 님들의 rds 루트 유저 이름
#    password: 님들의 rds 비밀번호
#    driver-class-name: com.mysql.cj.jdbc.Driver
#  sql:
#    init:
#      mode: never
#  jpa:
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
#        show_sql: true
#        format_sql: true
#        use_sql_comments: true
#        hbm2ddl:
#          auto: update
#        default_batch_fetch_size: 1000